/* Simple config.h file.
 *
 * The geocode.cc file includes <config.h> as its first include, so that
 * packages which include this file do not need to modify it to satisfy the
 * common idiom that all source files should include a packages config.h file
 * as their first include.
 *
 * The only
 */

#define rare(X) X
